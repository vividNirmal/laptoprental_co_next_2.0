import Addredirect from "@/components/page/redirect/addredirect";
import React from "react";

function RedirectUpdate({params}) {
  return <Addredirect id={params.id} />;
}

export default RedirectUpdate;
